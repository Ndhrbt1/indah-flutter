part of '../_index.dart';

class BerandaDelta extends StatelessWidget {
  const BerandaDelta({Key? key}) : super(key: key);

  BerandaCtrl get ct => Ctrl.beranda;
  BerandaData get dt => Data.beranda.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
