part of '../_index.dart';

class FoodDelta extends StatelessWidget {
  const FoodDelta({Key? key}) : super(key: key);

  FoodCtrl get ct => Ctrl.food;
  FoodData get dt => Data.food.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
