part of '../_index.dart';

class TechDelta extends StatelessWidget {
  const TechDelta({Key? key}) : super(key: key);

  TechCtrl get ct => Ctrl.tech;
  TechData get dt => Data.tech.st;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
